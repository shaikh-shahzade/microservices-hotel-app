spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel-service-micro
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect

  application:
    name: HOTEL-SERVICE

server:
  port: 8082